{
  "name": "nao4you",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "git config core.hooksPath .husky"
  },
  "lint-staged": {
    "*.{js, json}": ["biome check --write", "biome format --write"]
  },

  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0"
  }
}
